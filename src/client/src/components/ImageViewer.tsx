/**
 * Image Viewer Widget (Main Export)
 * This maintains backward compatibility while using the new modular architecture
 */

import ImageViewerWidget from './ImageViewer/index';

// Export the modular version as both default and named export
export default ImageViewerWidget;
export { ImageViewerWidget };