# Webpack Dev Server Configuration
# Copy this file to .env to enable proper webpack dev server operation

# Fix for "allowedHosts[0] should be a non-empty string" error
WDS_SOCKET_HOST=localhost
WDS_SOCKET_PORT=3000

# Disable ESLint plugin for faster builds (optional)
DISABLE_ESLINT_PLUGIN=true
