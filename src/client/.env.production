# Production Build Configuration
# Suppress source maps for smaller builds
GENERATE_SOURCEMAP=false

# Suppress CSS minifier warnings for nested calc() in Radix UI
# These are harmless warnings from postcss-calc with modern CSS
DISABLE_CSS_MINIFIER_WARNINGS=true
